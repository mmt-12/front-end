name: frontend-ci
on:
  push:
    branches: [ "develop", "main" ]

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Docker login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set tags (main -> prod, otherwise dev)
        id: tags
        run: |
          IMAGE=ghcr.io/mmt-12/memento-web
          CHAN=$([ "${{ github.ref_name }}" = "main" ] && echo "prod" || echo "dev")
          echo "image=$IMAGE" >> $GITHUB_OUTPUT
          echo "chan=$CHAN"   >> $GITHUB_OUTPUT
          echo "sha=${{ github.sha }}" >> $GITHUB_OUTPUT

      - name: Build & Push (Dockerfile at docker/app/Dockerfile)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./docker/app/Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ${{ steps.tags.outputs.image }}:${{ steps.tags.outputs.chan }}
            ${{ steps.tags.outputs.image }}:${{ steps.tags.outputs.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          # 필요하면 빌드 ARG 전달 (예: API_BASE_URL 등)
          # build-args: |
          #   VITE_API_BASE_URL=https://api.example.com